[pytest]

markers =
    slow: marks tests that run real model inference (deselect with -m "not slow")
    whisper: marks tests that use the Whisper model
    canary: marks tests that use the Canary model
    parakeet: marks tests that use the Parakeet model

addopts = -v --tb=short

testpaths = app/tests
